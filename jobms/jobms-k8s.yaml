apiVersion: apps/v1
kind: Deployment
metadata:
  name: jobms
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jobms
  template:
    metadata:
      labels:
        app: jobms
    spec:
      containers:
        - name: jobms
          image: donsaifulban1/jobms
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_DATASOURCE_URL
              value: jdbc:postgresql://postgres:5432/jobmsdb
            - name: SPRING_DATASOURCE_USERNAME
              value: postgres
            - name: SPRING_DATASOURCE_PASSWORD
              value: root
            - name: RABBITMQ_HOST
              value: rabbitmq
            - name: SPRING_CONFIG_IMPORT
              value: "optional:configserver:http://config-server:8888/"
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: http://service-registry:8761/eureka/
            - name: MANAGEMENT_ZIPKIN_TRACING_ENDPOINT
              value: http://zipkin:9411/api/v2/spans
---
apiVersion: v1
kind: Service
metadata:
  name: jobms
spec:
  selector:
    app: jobms
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
